# 工单 9 — 市场约束报告可读性改造：读者正文 + 审计附录 + 语义 lint

> 执行对象：Claude Code  
> 当前阶段：生产报告格式改造，不是数据管道或评分模型改造  
> 背景：`reports/` 中的报告已具备冻结快照、机械打分、数据质量披露、证伪条件和机器可读 sidecar，但正文同时混入了程序字段、运行日志、协议自证和多套比喻，导致中文不自然，且偶尔从 L2 价格形状越界到 L3 原因断言。

---

## 0. 执行约束（违反任一条 = 本单拒收）

### 0.1 只允许修改

- `skills/market-constraint-auditor/SKILL.md`
- `skills/market-constraint-auditor/references/market-constraint-protocol.md`
- `scripts/report_lint.py`
- `CHANGES.md`（只追加本轮验收记录）

### 0.2 运行时镜像同步

仓库修改验收通过后，同步下列两份到实际运行时目录：

- `~/.agents/skills/market-constraint-auditor/SKILL.md`
- `~/.agents/skills/market-constraint-auditor/references/market-constraint-protocol.md`

`~/.claude/skills/market-constraint-auditor` 当前是指向上述目录的符号链接，不得破坏或替换链接。完成后必须用 `cmp -s` 证明仓库副本与运行时副本逐字节一致。

### 0.3 一律不动

- 不重写任何历史 `reports/*.md/.html/.pdf/.json`。
- 不修改 `data/` 下任何文件。
- 不修改行情抓取、快照、统计、打分、证伪检查和 HTML/PDF 转换逻辑。
- 不修改 `data/regime_matrix.json` 或任何评分阈值。
- 不新增模型调用、网络依赖或第三方包。
- 不 git commit。

### 0.4 测试约束

- Python 统一使用 `/Users/jowang/miniconda3/bin/python3`。
- 所有合成报告和临时 sidecar/ledger 都写入 `tmp` 目录，不进入 `reports/` 和 `data/`。
- 测试前后必须证明下列文件 SHA256 不变：
  - `data/timeseries.jsonl`
  - `data/diagnosis_log.jsonl`
  - `data/open_threads.jsonl`
  - `data/plumbing.jsonl`

---

## 1. 本轮要解决什么

### 1.1 产品定位

这些报告不只是模型调试产物，而是每日给中文母语、非专业交易员背景读者使用的市场诊断。第一阅读目标是让读者迅速回答：

1. 今天的跨资产价格组合最像什么？
2. 这个判断到底是价格事实，还是对原因的推断？
3. 有哪些事实支持，又有哪些仍然不知道？
4. 下一个交易日什么观察会改变判断？

机械打分、字段名、协议条款、数据源异常和运行状态仍需完整保留，但它们应进入「审计附录」，不得抢占读者正文。

### 1.2 必须达成的目标

1. Schema A 改为「读者正文 + 审计附录」两层结构。
2. 读者正文里不再出现程序字段直译、混乱比喻和运行日志。
3. 当 L3 信心为 ★☆☆ 或存在观测等价的备选原因时，正文必须写「最佳匹配的价格模式」和「原因未确认」，不得直接声称「主导约束已确立」。
4. 限制从共同波动到因果的越级，特别是油价→通胀→利率、名义利率→实际利率以及 MOVE→基金实际买卖的断言。
5. `report_lint.py` 不仅检查机器结构，还能拒绝明确的读者正文退化。
6. 旧报告不回溯改写，新 lint 不破坏历史报告的旧格式验证。

### 1.3 非目标

- 不追求「更好听」或更像财经自媒体。
- 不把报告改成强制看多/看空或交易建议。
- 不用减少数据披露换取表面简洁。
- 不重新设计 Schema D 的分析逻辑；只把它的中文改自然。
- 不在本轮建立 `reports/index.md` 或清理 `-kimi`/副本/盘中报告；报告目录治理另立工单。

### 1.4 成功标准

一位不知道 `signal` / `vol` / `consec` / `stale` / `regime_row` 的中文读者，应能在 90 秒内读完正文，并准确说出：当日价格形状、主要不确定性、下一个确认条件。同时，审计人员仍可在附录里找到全部机械打分、数据质量、人工偏离、完整证据表和证伪阈值。

---

## 2. 角色与责任

| 角色 | 需要完成的任务 | 不应被迫承担的任务 |
|---|---|---|
| 普通读者 | 理解今日价格组合、不确定性和下一观察点 | 解码程序字段、协议编号或运行故障 |
| 报告作者（LLM） | 先完成机械输入审计，再把结果翻译为读者正文 | 为了填满「机制」而补写无法证实的原因 |
| 审计/运维人员 | 查阅数据日、源状态、机械打分、偏离和阈值 | 从被压缩的读者段落里反推审计过程 |
| `report_lint.py` | 检查格式与明确可机械判定的语言违规 | 代替人类判断所有因果关系是否成立 |

---

## 3. 新的端到端流程

1. 读取冻结快照、统计、管道数据、机械打分和悬而未决问题。
2. 先在内部完成现有协议的 L1/L2/L3/L4 判定、Schema A/D 选择和人工偏离检查。
3. 根据第 4 节的「表达状态」选择正文措辞，先生成读者正文。
4. 再写审计附录，完整放入机械字段、数据异常、评分偏离和全量证据。
5. 保持现有 Markdown 落盘、threshold sidecar、diagnosis ledger、open threads 和 HTML/PDF 转换流程不变。
6. 运行 `report_lint.py`：先检查原有机器结构，再检查新的读者正文规则。
7. lint 通过才视为报告生成完成；lint 失败必须重写报告，不得仅在 CHANGES 中记录后放行。

---

## 4. 表达状态设计

原有 A/D schema 不变，但 Schema A 增加三种「表达状态」。这些是写作状态，不是新的评分行，不写入 `regime_matrix.json`。

| 状态 | 条件 | 正文必须怎么写 | 正文禁止怎么写 |
|---|---|---|---|
| `D_ABSTAIN` | 命中现有 Schema D 规则 | 「今天不判断主导原因」+纯价格事实+何时重评 | 机制、交易建议、「主导约束是 X」 |
| `A_PRICING_ONLY` | L2 ≥ ★★☆，但 L3 = ★☆☆；或存在观测等价的备选原因 | 「最佳匹配的价格模式是 X，原因未确认」 | 「X 接过定价权」、「X 已坐实」、「真 X 定价」 |
| `A_NARRATIVE_SUPPORTED` | L3 ≥ ★★☆，具有持续性/广度，且备选原因已有区分证据 | 允许「当前更支持 X 作为主要解释」，但仍需给出证伪 | 任何 L4 断言或无来源因果 |

额外规则：

- 报告头部的「主导约束」字段可保留，以免破坏现有归档习惯。
- `A_PRICING_ONLY` 时头部必须写成：  
  `**主导约束：** 原因未确认（L2 最佳匹配：P — 宽松交易）`
- 文件名、ledger 的 `regime` / `regime_row` 和 threshold sidecar 继续按现有机器判断写入，不因读者措辞改变。

---

## 5. Schema A 新输出结构

`market-constraint-protocol.md` 中的 Schema A 替换为下列结构。标题文字固定，便于 lint 和后续自动处理。

```markdown
## 今日结论

- **最佳匹配的价格模式：** ...
- **原因判断：** 已获得支持 / 未确认 / 两种解释暂不可分
- **确定性：** 价格层 ... / 原因层 ...

[用 2–4 个短句说清价格事实、最佳匹配与主要不确定性。]

## 与上一个交易日相比

- [最多 3 条，只写真正改变判断的增量]

## 核心证据

| 观察 | 当日事实 | 对判断的影响 |
|---|---|---|
| ... | ... | 支持 / 反对 / 暂无区分力 |

[最多 5 行，只放承重证据。]

## 当前不能确定的事

- [1–3 条，明确是数据缺失、原因无法区分，还是现实层无数据]

## 下一个确认点

- [最多 3 条：具体变量 + 方向/阈值 + 改变什么判断]

## 审计附录

### 数据口径与运行状态
[fetched_at、数据日、滞后/断档/降级、运行故障]

### 机械打分与人工判断
[best row、match%、aligned/conflicted、Schema 建议、偏离理由]

### 完整证据表
[全量资产、波动分位、连续方向、as_of、支持/冲突/不作证据]

### 原因候选与区分证据
[对各候选原因分别说明：已有什么证据、缺什么、是否可区分]

### 证伪条件
[与 thresholds sidecar 逐条对齐]

### 待确认问题
[对应 open_threads，使用「待确认/已回答/顺延/过期」，不用「悬案/立案/收案」]

### 术语说明
[只放本日实际用到的 3–6 项]
```

### 5.1 正文长度

- 「审计附录」之前的全部读者正文，中文字符不得超过 1500 个。
- 「核心证据」最多 5 行。
- 「与上一个交易日相比」和「下一个确认点」各最多 3 条。
- 审计附录不设硬长度限制，但不得重复正文的整段表述。

### 5.2 Schema D 调整

Schema D 仍然短于 Schema A，不增加「原因候选」、「机制」、「观察清单」或完整审计附录。只做中文化：

- 「不足支撑」改为「为什么今天不判断原因」。
- 「盘面纯描述」保留，表中用「显著波动/波动过小/数据滞后」，不用 `signal/vol/stale`。
- 「何时重评」保留，必须是可观测阈值。
- 机械打分如必须披露，放在末尾一个「技术说明」短段，不得穿插在读者说明中。

---

## 6. 中文语言硬规则

### 6.1 程序字段隔离

在「审计附录」之前，禁止出现：

- `signal`、`signal-grade`、`vol`、`consec`、`stale`、`callable`
- `aligned`、`conflicted`、`whipsaw`、`regime`、`regime_row`
- `mark`、`denominator`、`gap_adjacent`
- 「可召唤」、「判别腿」、「风险腿」、「美元腿」、「长端腿」、「机器腿」、「冲突腿」、「确认腿」
- 「深噪」、「噪音门」、「厚盘」、「独木」

可以在审计附录保留原始字段名，但第一次出现必须有中文语义，例如：

> 达到正式判断门槛（`callable=true`）

不得把原始字段名当成中文句子的谓语或定语。

### 6.2 固定替换表

| 禁用/不推荐 | 统一改为 |
|---|---|
| `signal 级` | 显著波动（首次说明为超过自身历史波动的第 50 百分位） |
| `vol 68` | 波动幅度处于自身历史第 68 百分位 |
| `consec -3` | 连续 3 个交易日下跌 |
| `stale` | 数据滞后（截至前一交易日/具体日期） |
| `XX 腿` | XX 一侧的证据 / XX 证据组 |
| 悬案/立案/裁决/收案 | 待确认问题/新增问题/本日结论/已回答 |
| 管道数量层 | 资金市场官方数据 |
| 承接测试 | 下一交易日的持续性验证 |
| 换了主角/接过定价权 | 相比上一交易日，X 的匹配度上升 |
| 复活/熄火/瘫着/挨打 | 重新出现/减弱/波动过小/下跌 |
| 坐实 | 得到进一步支持 |

### 6.3 比喻系统

- 读者正文原则上不使用法庭、戏剧、人体、管道和机器的拟人化比喻。
- 「价格模式」「证据」「支持」「反对」「未确认」「下一验证」是默认词汇。
- 审计附录也优先用字面意义；只有引用历史问题 ID 时可保留旧词。

### 6.4 句子与排版

- 一个句子只承担一个主要推理步骤。
- 正文中不用 `A → B → C` 代替因果论证。
- 括号只补充必要数字或日期，不在括号里放第二条论证。
- 读者正文不出现协议编号（F10/F12/§3）、布尔字段或实现词。
- 粗体只用于小标题关键词或一个数据结论，不用于整句加压。

### 6.5 运行日志隔离

「补发说明」、「无头进程」、「墙钟看门狗」、「挂死」、「例行自愈」、「跑批」等运行信息，只能出现在「审计附录 > 数据口径与运行状态」。如果运行事故影响了数据完整性，正文只写对判断的结果，例如：

> 部分商品数据暂不可用，因此今日不对商品方向作结论。

---

## 7. 因果表达门

### 7.1 基本原则

价格同时波动只能证明「组合形状与某个矩阵行一致」，不能单独证明因果。

只有下列任一类证据存在时，才允许在「原因判断」中使用明确因果语言：

1. 已指名的具体新闻/政策/事件，且时间顺序与价格反应相符。
2. 协议已定义的 L1 机械阈值被当日新鲜数据命中，并且补充证据同向。
3. 官方资金市场数据提供直接证据，例如 SRF 非零或 SOFR−IORB 持续为正。
4. 利率分解数据明确指向实际利率或通胀补偿。
5. 用户提供了额外的可验证因果证据。

不满足以上条件时，只允许使用：

- 「与 X 价格模式一致」
- 「最接近 X」
- 「同时出现」
- 「这两种解释在当日数据上不可分」
- 「原因未确认」

### 7.2 利率归因硬门

当 loader 中 `rates_attribution` 缺位、滞后或不可用时：

- 只能写「名义利率上升/下降，来源未分解」。
- 禁止写「实际利率上行/回落」。
- 禁止写「油价上涨/回落推动了利率变动」。
- 禁止写「通胀燃料被移走」、「实际利率挤压松开」等等价拟人化因果。

这条不仅适用于读者正文，也适用于审计附录的分析文字。附录可以原样展示数据字段，但不得用它们支持数据没有给出的归因。

### 7.3 MOVE 表达边界

- MOVE 命中协议阈值时，可写「程序化资金减仓/加仓的可能性上升」。
- 不得仅凭 MOVE 就断言「风险平价基金已被迫卖出某资产」，因为本系统没有真实持仓和成交流数据。
- 因此「MOVE 是机械减仓链条的驱动源」必须改为「MOVE 上升与程序化减仓风险一致，但本系统不直接观察基金持仓」。

---

## 8. `report_lint.py` 改造

### 8.1 兼容性

- 原有的 Schema A/D、sidecar、ledger、机械分数和证伪检查一律保留。
- 新语言规则默认只对报告日期 `>= 20260711` 生效。旧报告仍按旧结构检查，不得因新规则全量报错。
- 增加 `--style-only` 参数，只运行新的结构/语言检查，用于 tmp 合成样例；不读 sidecar 和 ledger。
- 现有 `--report` 用法和返回码不变。

### 8.2 新增检查项

Schema A（新格式）至少输出：

| lint item | PASS 条件 |
|---|---|
| `reader_sections` | 「今日结论/与上一交易日相比/核心证据/当前不能确定的事/下一个确认点/审计附录」全部存在且顺序正确 |
| `reader_length` | 审计附录前中文字符数 ≤ 1500 |
| `reader_internal_terms` | 正文不含第 6.1 节的内部字段/直译词 |
| `reader_metaphors` | 正文不含「换了主角/接过定价权/余波/残响/复活/熄火/瘫着/挨打/出场作证/收案」 |
| `reader_ops_isolation` | 运行日志词不出现在审计附录之前 |
| `pricing_only_language` | L3=★☆☆ 时同时存在「最佳匹配的价格模式」和「原因未确认/暂不可分」，且不含「主导约束已确立/坐实/接过定价权」 |
| `rates_attribution_guard` | 文中明示利率分解缺位时，后文不得声称实际利率方向或油价→利率因果 |
| `schema_d_plain_language` | Schema D 正文不含内部字段词，仍不含机制/观察清单 |

### 8.3 检查边界

- 正文区域定义为文档开头到首个 `## 审计附录` 之前。
- 引号或代码块不需为历史原句开绿灯：新报告正文本就不应引用黑话。
- lint 只对可明确用字符串/结构判定的规则负责。不要为了「检测所有因果越界」写一个复杂、不稳定的中文 NLP 分类器。
- 每个失败项必须输出具体命中词或缺失标题，不得只输出「style failed」。

---

## 9. 开发顺序

1. 先修改 `market-constraint-protocol.md` 的 Schema A/D 和因果表达规则，冻结新的输出合同。
2. 再修改 `SKILL.md` 的生成流程与中文写作纪律，不要在两份文档里写出互相矛盾的模板。
3. 修改 `report_lint.py`，保留原有检查，新增带日期切换的 style v2 检查。
4. 用 tmp 合成样例跑正反测试。
5. 回归检查 20260703–20260710 六份现有报告：旧规则仍按原样通过，style v2 不对它们强制生效。
6. 同步两份运行时文档，用 `cmp -s` 验证。
7. 追加 `CHANGES.md`，记录改动、正反样例和验收输出。

---

## 10. 验收样例

### 10.1 必须 PASS 的 Schema A 正文样例

```markdown
## 今日结论

- **最佳匹配的价格模式：** 宽松交易。
- **原因判断：** 未确认。
- **确定性：** 价格层中等，原因层低。

7月9日收盘，股票、短期国债和贵金属同步上涨，原油下跌。这一组合最接近宽松交易，但当日没有已确认的政策事件，也可能是连续下跌后的短线回补。

## 与上一个交易日相比

- 股指从下跌转为普遍上涨。
- 油价回落，前一日的能源压力减弱。

## 核心证据

| 观察 | 当日事实 | 对判断的影响 |
|---|---|---|
| 美股 | 三大指数均显著上涨 | 支持宽松交易 |
| 短期国债 | SHY 上涨 | 支持短端利率预期下调 |
| 贵金属 | 黄金和白银同步上涨 | 支持，但不能单独确认原因 |
| MOVE | 历史基线存在异常 | 暂不用于判断 |

## 当前不能确定的事

- 无法区分利率预期真正转向与短线回补。
- 利率分解数据缺位，名义利率回落的来源未分解。

## 下一个确认点

- 如果 SHY 和主要股指连续第二日上涨，宽松交易得到进一步支持。
- 如果股指和 SHY 同时转跌，当日上涨更可能只是短线修复。

## 审计附录
...
```

### 10.2 必须 FAIL 的黑话样例

```markdown
## 今日结论

P 换了主角，利率腿 signal 级回归，MOVE 机器腿熄火，贵金属承接坐实宽松 regime。
```

预期：至少命中 `reader_internal_terms` 和 `reader_metaphors`。

### 10.3 必须 FAIL 的利率归因矛盾样例

```markdown
利率分解数据今日缺位，名义利率回落的来源未分解。

油价回落移走了通胀燃料，实际利率挤压随之松开。
```

预期：命中 `rates_attribution_guard`，报告 lint 失败。

### 10.4 必须 PASS 的附录机器字段样例

```markdown
## 审计附录

### 机械打分与人工判断

最佳矩阵行为宽松交易，匹配率 60.0%。共有 5 类资产同向（`aligned`），1 类资产反向（`conflicted`）。
```

预期：机器字段位于审计附录，且有中文解释，不触发正文违规。

---

## 11. 完整验收清单

Claude 必须逐项实测，并把命令和输出摘要追加到 `CHANGES.md`。

1. **协议合同**
   - Schema A 已分成读者正文与审计附录。
   - Schema D 仍为不诊断，没有被扩张成另一份 Schema A。
   - L2/L3/L4 范围和现有证伪规则未被放宽。
2. **读者结构**
   - 合成 Schema A 的 6 个固定一级段落齐全、顺序正确。
   - 正文中文字符 ≤ 1500，核心证据 ≤ 5 行。
3. **语言正反例**
   - 第 10.1 节 PASS。
   - 第 10.2 节 FAIL，输出具体黑话。
   - 运行日志出现在正文时 FAIL，移入附录后 PASS。
4. **原因边界**
   - L3=★☆☆ 且写「原因未确认」的样例 PASS。
   - L3=★☆☆ 但写「主导约束已坐实」的样例 FAIL。
   - 第 10.3 节利率归因矛盾 FAIL。
   - 利率分解有效，并在同段附 `rates_attribution` 结果的合成样例 PASS。
5. **新旧兼容**
   - 20260703、20260705、20260707、20260708、20260709、20260710 六份报告按默认 lint 路径仍能运行现有检查，不强制 style v2。
   - 文件名为 `20260711--...` 的合成报告默认触发 style v2。
   - `--style-only` 不读真实 ledger/sidecar。
6. **原有 lint 不退化**
   - Schema A 仍检查 sidecar、ledger、数字机械分数、证伪章节。
   - Schema D 仍检查 ledger，仍禁止机制/观察清单。
7. **运行时一致性**
   - 仓库 `SKILL.md` vs `~/.agents/.../SKILL.md`：`cmp -s` 返回 0。
   - 仓库 protocol vs `~/.agents/.../market-constraint-protocol.md`：`cmp -s` 返回 0。
   - `~/.claude/skills/market-constraint-auditor` 仍是原符号链接。
8. **静态与边界证据**
   - `py_compile scripts/report_lint.py` PASS。
   - 四个受保护 `data/` 文件测试前后 SHA256 一致。
   - `git diff --name-only` 只包含第 0.1 节允许的四个跟踪文件（工作区原有改动必须单独列出，不冒充本轮改动）。
   - 不 commit。

---

## 12. 风险和实现注意

1. **不要把简化错做成删减证据。** 完整数据和机械输出必须转移到附录，不是消失。
2. **不要让附录再写一遍全文。** 正文是结论和承重证据，附录是数据系谱与审计细节。
3. **不要用更多括号来「解释」黑话。** 应该替换黑话，而不是写成「判别腿（即区分证据）」。
4. **不要用 lint 代替分析判断。** 因果门的主体仍是写作协议；lint 只守能确定的底线。
5. **不放宽 Schema D。** 当证据不足时，不诊断仍比一份「语言更自然但原因更虚」的 Schema A 更好。
6. **注意报告日与数据日不同。** style v2 的日期切换使用文件名中的报告日，不从正文的数据时间推断。

---

## 13. 交付回报格式

Claude 执行完后的最终回报必须包含：

1. 实际修改文件列表。
2. Schema A/D 输出合同改动摘要。
3. 新增 lint item 清单。
4. 第 11 节每一项验收的 PASS/FAIL 与命令摘要。
5. 受保护数据文件测试前后 SHA256。
6. 仓库/运行时两份文档的 `cmp -s` 结果。
7. `git diff --name-only` 和 `git status --short`，并区分本轮改动与工作区原有改动。
8. 任何未完成项或运行时同步失败必须明确报告，不得用「基本完成」等模糊语句代替。
